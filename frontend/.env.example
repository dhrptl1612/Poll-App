# Example environment variables for frontend

# API URL for production deployment
# VITE_API_URL=https://your-backend-api-url.com

# Leave empty to use relative URLs (when frontend and backend are served from same origin)
VITE_API_URL=
